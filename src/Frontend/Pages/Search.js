import { SearchPageContainer } from '../Components/Container';

// Function to output search page (landing page).
export default function SearchPage(){
    return(
        <SearchPageContainer/> // Container that is assembled through Components.js.
    );
}