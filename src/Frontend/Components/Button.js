import Button from '@mui/material/Button';
import styled from 'styled-components';

const ButtonDiv = styled.div`
    margin-top: 25px;
`

export default function SubmitButton() {
  return (
    <ButtonDiv>
      <Button variant="contained">gdfgdfdf</Button>
    </ButtonDiv>
  );
}