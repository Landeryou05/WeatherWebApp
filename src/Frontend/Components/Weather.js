import styled from "styled-components";

const WeatherDiv = styled.div`
    padding: 25px;
    margin-bottom: 25px;
`;

export default function Weather(){
    return (
        <WeatherDiv>
            DISPLAY WEATHER
        </WeatherDiv>
    );
}